{
  "name": "nuxt-kudra-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "publish": "lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^5.6.1",
    "rimraf": "^3.0.2"
  }
}
